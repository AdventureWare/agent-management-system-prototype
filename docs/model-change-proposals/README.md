# Model Change Proposals

Use this directory for proposed changes to AMS domain constructs before implementation.

Create a proposal when a task would add or change an entity, field, relationship, enum, status, type, lifecycle, or core concept. Use `TEMPLATE.md`, compare against the glossary and ontology, then update source-of-truth docs and tests only after the decision is accepted or explicitly marked experimental.

