The intrusive full reload is gone on [src/routes/app/sessions/+page.svelte](/Users/colinfreed/Projects/Experiments/agent-management-system-prototype/src/routes/app/sessions/+page.svelte#L1). Auto-refresh now does an in-place SvelteKit data refresh instead of `window.location.reload()`, so the page no longer blows away what the user is doing. I also constrained it so it only runs when there are active sessions, skips while the user is focused in an input/textarea/select or the tab is hidden, and added a manual `Refresh now` button near the toggle at [src/routes/app/sessions/+page.svelte](/Users/colinfreed/Projects/Experiments/agent-management-system-prototype/src/routes/app/sessions/+page.svelte#L79).

Verification: `npm run check` passed. `npm run lint` still fails because of pre-existing Prettier issues in generated files under `data/agent-sessions/...`, not because of the Svelte change.